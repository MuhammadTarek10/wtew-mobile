import 'package:flutter/material.dart';

class GPACalculatorView extends StatelessWidget {
  const GPACalculatorView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Asd"),
      ),
    );
  }
}
